export class Season {
    constructor(public id: number, public imgUrl: string, public title: string, public link: string ) {
        this.id = id;
        this.imgUrl = imgUrl;
        this.title = title;
        this.link = link;
    }
}